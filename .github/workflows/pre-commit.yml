name: Pre-commit

on:
  push:
  pull_request:

jobs:
  pre-commit:
    name: Pre-commit hooks
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6.0.2

      - name: Run pre-commit
        run: |
          make fmt
